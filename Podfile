# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SteemShutter Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SteemShutter Extension
pod 'SwiftyJSON'

end

target 'SteemShutter Framework' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SteemShutter Framework
pod 'SwiftyJSON'

end

target 'SteemShutter v3.0' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SteemShutter v3.0
pod 'SwiftyJSON'

end
